[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP Server for Todo Manager - provides tools for AI agent to interact with todo backend"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.25.0",
]
